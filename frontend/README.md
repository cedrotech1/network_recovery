# UoK Network Recovery Frontend

React + Vite + Tailwind UI for the **Automatic Failure Detection and Recovery System** — University of Kigali LAN laboratory prototype.

Configure `VITE_API_BASE_URL` in `.env` (default `http://localhost:9500/api/v1`).
